{
  "name": "claude-linear-sync",
  "version": "1.0.0",
  "description": "Claude Code session to Linear sync daemon using hooks",
  "type": "module",
  "bin": {
    "claude-linear-sync": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "npx tsx src/cli.ts",
    "start": "node dist/cli.js start",
    "postinstall": "npm run build || true"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "commander": "^13.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
